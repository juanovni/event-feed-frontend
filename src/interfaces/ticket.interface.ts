import { Event } from "./event.interface";

export interface Ticket {
  //id: string;
  event: Event;
  /*   cost: number;
    ticketNumber: string;
    seat?: string;
    section?: string; */
  //createdAt: Date;
}
